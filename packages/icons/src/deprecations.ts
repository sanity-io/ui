/* THIS FILE IS AUTO-GENERATED – DO NOT EDIT */

/**
 * @deprecated `AccessDeniedIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {AccessDeniedIcon} from '@sanity/icons/AccessDenied'`
 */
export declare const AccessDeniedIcon: never

/**
 * @deprecated `ActivityIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {ActivityIcon} from '@sanity/icons/Activity'`
 */
export declare const ActivityIcon: never

/**
 * @deprecated `AddCircleIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {AddCircleIcon} from '@sanity/icons/AddCircle'`
 */
export declare const AddCircleIcon: never

/**
 * @deprecated `AddCommentIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {AddCommentIcon} from '@sanity/icons/AddComment'`
 */
export declare const AddCommentIcon: never

/**
 * @deprecated `AddDocumentIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {AddDocumentIcon} from '@sanity/icons/AddDocument'`
 */
export declare const AddDocumentIcon: never

/**
 * @deprecated `AddIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {AddIcon} from '@sanity/icons/Add'`
 */
export declare const AddIcon: never

/**
 * @deprecated `AddUserIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {AddUserIcon} from '@sanity/icons/AddUser'`
 */
export declare const AddUserIcon: never

/**
 * @deprecated `ApiIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {ApiIcon} from '@sanity/icons/Api'`
 */
export declare const ApiIcon: never

/**
 * @deprecated `ArchiveIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {ArchiveIcon} from '@sanity/icons/Archive'`
 */
export declare const ArchiveIcon: never

/**
 * @deprecated `ArrowDownIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {ArrowDownIcon} from '@sanity/icons/ArrowDown'`
 */
export declare const ArrowDownIcon: never

/**
 * @deprecated `ArrowLeftIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {ArrowLeftIcon} from '@sanity/icons/ArrowLeft'`
 */
export declare const ArrowLeftIcon: never

/**
 * @deprecated `ArrowRightIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {ArrowRightIcon} from '@sanity/icons/ArrowRight'`
 */
export declare const ArrowRightIcon: never

/**
 * @deprecated `ArrowTopRightIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {ArrowTopRightIcon} from '@sanity/icons/ArrowTopRight'`
 */
export declare const ArrowTopRightIcon: never

/**
 * @deprecated `ArrowUpIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {ArrowUpIcon} from '@sanity/icons/ArrowUp'`
 */
export declare const ArrowUpIcon: never

/**
 * @deprecated `AsteriskIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {AsteriskIcon} from '@sanity/icons/Asterisk'`
 */
export declare const AsteriskIcon: never

/**
 * @deprecated `BarChartIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {BarChartIcon} from '@sanity/icons/BarChart'`
 */
export declare const BarChartIcon: never

/**
 * @deprecated `BasketIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {BasketIcon} from '@sanity/icons/Basket'`
 */
export declare const BasketIcon: never

/**
 * @deprecated `BellIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {BellIcon} from '@sanity/icons/Bell'`
 */
export declare const BellIcon: never

/**
 * @deprecated `BillIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {BillIcon} from '@sanity/icons/Bill'`
 */
export declare const BillIcon: never

/**
 * @deprecated `BinaryDocumentIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {BinaryDocumentIcon} from '@sanity/icons/BinaryDocument'`
 */
export declare const BinaryDocumentIcon: never

/**
 * @deprecated `BlockContentIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {BlockContentIcon} from '@sanity/icons/BlockContent'`
 */
export declare const BlockContentIcon: never

/**
 * @deprecated `BlockElementIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {BlockElementIcon} from '@sanity/icons/BlockElement'`
 */
export declare const BlockElementIcon: never

/**
 * @deprecated `BlockquoteIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {BlockquoteIcon} from '@sanity/icons/Blockquote'`
 */
export declare const BlockquoteIcon: never

/**
 * @deprecated `BoldIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {BoldIcon} from '@sanity/icons/Bold'`
 */
export declare const BoldIcon: never

/**
 * @deprecated `BoltIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {BoltIcon} from '@sanity/icons/Bolt'`
 */
export declare const BoltIcon: never

/**
 * @deprecated `BookIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {BookIcon} from '@sanity/icons/Book'`
 */
export declare const BookIcon: never

/**
 * @deprecated `BookmarkFilledIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {BookmarkFilledIcon} from '@sanity/icons/BookmarkFilled'`
 */
export declare const BookmarkFilledIcon: never

/**
 * @deprecated `BookmarkIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {BookmarkIcon} from '@sanity/icons/Bookmark'`
 */
export declare const BookmarkIcon: never

/**
 * @deprecated `BottleIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {BottleIcon} from '@sanity/icons/Bottle'`
 */
export declare const BottleIcon: never

/**
 * @deprecated `BugIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {BugIcon} from '@sanity/icons/Bug'`
 */
export declare const BugIcon: never

/**
 * @deprecated `BulbFilledIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {BulbFilledIcon} from '@sanity/icons/BulbFilled'`
 */
export declare const BulbFilledIcon: never

/**
 * @deprecated `BulbOutlineIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {BulbOutlineIcon} from '@sanity/icons/BulbOutline'`
 */
export declare const BulbOutlineIcon: never

/**
 * @deprecated `CalendarIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {CalendarIcon} from '@sanity/icons/Calendar'`
 */
export declare const CalendarIcon: never

/**
 * @deprecated `CaseIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {CaseIcon} from '@sanity/icons/Case'`
 */
export declare const CaseIcon: never

/**
 * @deprecated `ChartUpwardIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {ChartUpwardIcon} from '@sanity/icons/ChartUpward'`
 */
export declare const ChartUpwardIcon: never

/**
 * @deprecated `CheckmarkCircleIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {CheckmarkCircleIcon} from '@sanity/icons/CheckmarkCircle'`
 */
export declare const CheckmarkCircleIcon: never

/**
 * @deprecated `CheckmarkIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {CheckmarkIcon} from '@sanity/icons/Checkmark'`
 */
export declare const CheckmarkIcon: never

/**
 * @deprecated `ChevronDownIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {ChevronDownIcon} from '@sanity/icons/ChevronDown'`
 */
export declare const ChevronDownIcon: never

/**
 * @deprecated `ChevronLeftIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {ChevronLeftIcon} from '@sanity/icons/ChevronLeft'`
 */
export declare const ChevronLeftIcon: never

/**
 * @deprecated `ChevronRightIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {ChevronRightIcon} from '@sanity/icons/ChevronRight'`
 */
export declare const ChevronRightIcon: never

/**
 * @deprecated `ChevronUpIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {ChevronUpIcon} from '@sanity/icons/ChevronUp'`
 */
export declare const ChevronUpIcon: never

/**
 * @deprecated `CircleIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {CircleIcon} from '@sanity/icons/Circle'`
 */
export declare const CircleIcon: never

/**
 * @deprecated `ClipboardIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {ClipboardIcon} from '@sanity/icons/Clipboard'`
 */
export declare const ClipboardIcon: never

/**
 * @deprecated `ClipboardImageIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {ClipboardImageIcon} from '@sanity/icons/ClipboardImage'`
 */
export declare const ClipboardImageIcon: never

/**
 * @deprecated `ClockIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {ClockIcon} from '@sanity/icons/Clock'`
 */
export declare const ClockIcon: never

/**
 * @deprecated `CloseCircleIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {CloseCircleIcon} from '@sanity/icons/CloseCircle'`
 */
export declare const CloseCircleIcon: never

/**
 * @deprecated `CloseIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {CloseIcon} from '@sanity/icons/Close'`
 */
export declare const CloseIcon: never

/**
 * @deprecated `CodeBlockIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {CodeBlockIcon} from '@sanity/icons/CodeBlock'`
 */
export declare const CodeBlockIcon: never

/**
 * @deprecated `CodeIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {CodeIcon} from '@sanity/icons/Code'`
 */
export declare const CodeIcon: never

/**
 * @deprecated `CogIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {CogIcon} from '@sanity/icons/Cog'`
 */
export declare const CogIcon: never

/**
 * @deprecated `CollapseIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {CollapseIcon} from '@sanity/icons/Collapse'`
 */
export declare const CollapseIcon: never

/**
 * @deprecated `ColorWheelIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {ColorWheelIcon} from '@sanity/icons/ColorWheel'`
 */
export declare const ColorWheelIcon: never

/**
 * @deprecated `CommentIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {CommentIcon} from '@sanity/icons/Comment'`
 */
export declare const CommentIcon: never

/**
 * @deprecated `ComponentIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {ComponentIcon} from '@sanity/icons/Component'`
 */
export declare const ComponentIcon: never

/**
 * @deprecated `ComposeIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {ComposeIcon} from '@sanity/icons/Compose'`
 */
export declare const ComposeIcon: never

/**
 * @deprecated `ComposeSparklesIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {ComposeSparklesIcon} from '@sanity/icons/ComposeSparkles'`
 */
export declare const ComposeSparklesIcon: never

/**
 * @deprecated `ConfettiIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {ConfettiIcon} from '@sanity/icons/Confetti'`
 */
export declare const ConfettiIcon: never

/**
 * @deprecated `ControlsIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {ControlsIcon} from '@sanity/icons/Controls'`
 */
export declare const ControlsIcon: never

/**
 * @deprecated `CopyIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {CopyIcon} from '@sanity/icons/Copy'`
 */
export declare const CopyIcon: never

/**
 * @deprecated `CreditCardIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {CreditCardIcon} from '@sanity/icons/CreditCard'`
 */
export declare const CreditCardIcon: never

/**
 * @deprecated `CropIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {CropIcon} from '@sanity/icons/Crop'`
 */
export declare const CropIcon: never

/**
 * @deprecated `CubeIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {CubeIcon} from '@sanity/icons/Cube'`
 */
export declare const CubeIcon: never

/**
 * @deprecated `DashboardIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {DashboardIcon} from '@sanity/icons/Dashboard'`
 */
export declare const DashboardIcon: never

/**
 * @deprecated `DatabaseIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {DatabaseIcon} from '@sanity/icons/Database'`
 */
export declare const DatabaseIcon: never

/**
 * @deprecated `DesktopIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {DesktopIcon} from '@sanity/icons/Desktop'`
 */
export declare const DesktopIcon: never

/**
 * @deprecated `DiamondIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {DiamondIcon} from '@sanity/icons/Diamond'`
 */
export declare const DiamondIcon: never

/**
 * @deprecated `DocumentIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {DocumentIcon} from '@sanity/icons/Document'`
 */
export declare const DocumentIcon: never

/**
 * @deprecated `DocumentPdfIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {DocumentPdfIcon} from '@sanity/icons/DocumentPdf'`
 */
export declare const DocumentPdfIcon: never

/**
 * @deprecated `DocumentRemoveIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {DocumentRemoveIcon} from '@sanity/icons/DocumentRemove'`
 */
export declare const DocumentRemoveIcon: never

/**
 * @deprecated `DocumentSheetIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {DocumentSheetIcon} from '@sanity/icons/DocumentSheet'`
 */
export declare const DocumentSheetIcon: never

/**
 * @deprecated `DocumentTextIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {DocumentTextIcon} from '@sanity/icons/DocumentText'`
 */
export declare const DocumentTextIcon: never

/**
 * @deprecated `DocumentVideoIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {DocumentVideoIcon} from '@sanity/icons/DocumentVideo'`
 */
export declare const DocumentVideoIcon: never

/**
 * @deprecated `DocumentWordIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {DocumentWordIcon} from '@sanity/icons/DocumentWord'`
 */
export declare const DocumentWordIcon: never

/**
 * @deprecated `DocumentZipIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {DocumentZipIcon} from '@sanity/icons/DocumentZip'`
 */
export declare const DocumentZipIcon: never

/**
 * @deprecated `DocumentsIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {DocumentsIcon} from '@sanity/icons/Documents'`
 */
export declare const DocumentsIcon: never

/**
 * @deprecated `DotIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {DotIcon} from '@sanity/icons/Dot'`
 */
export declare const DotIcon: never

/**
 * @deprecated `DoubleChevronDownIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {DoubleChevronDownIcon} from '@sanity/icons/DoubleChevronDown'`
 */
export declare const DoubleChevronDownIcon: never

/**
 * @deprecated `DoubleChevronLeftIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {DoubleChevronLeftIcon} from '@sanity/icons/DoubleChevronLeft'`
 */
export declare const DoubleChevronLeftIcon: never

/**
 * @deprecated `DoubleChevronRightIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {DoubleChevronRightIcon} from '@sanity/icons/DoubleChevronRight'`
 */
export declare const DoubleChevronRightIcon: never

/**
 * @deprecated `DoubleChevronUpIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {DoubleChevronUpIcon} from '@sanity/icons/DoubleChevronUp'`
 */
export declare const DoubleChevronUpIcon: never

/**
 * @deprecated `DoubleQuoteIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {DoubleQuoteIcon} from '@sanity/icons/DoubleQuote'`
 */
export declare const DoubleQuoteIcon: never

/**
 * @deprecated `DownloadIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {DownloadIcon} from '@sanity/icons/Download'`
 */
export declare const DownloadIcon: never

/**
 * @deprecated `DragHandleIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {DragHandleIcon} from '@sanity/icons/DragHandle'`
 */
export declare const DragHandleIcon: never

/**
 * @deprecated `DropIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {DropIcon} from '@sanity/icons/Drop'`
 */
export declare const DropIcon: never

/**
 * @deprecated `EarthAmericasIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {EarthAmericasIcon} from '@sanity/icons/EarthAmericas'`
 */
export declare const EarthAmericasIcon: never

/**
 * @deprecated `EarthGlobeIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {EarthGlobeIcon} from '@sanity/icons/EarthGlobe'`
 */
export declare const EarthGlobeIcon: never

/**
 * @deprecated `EditIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {EditIcon} from '@sanity/icons/Edit'`
 */
export declare const EditIcon: never

/**
 * @deprecated `EllipsisHorizontalIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {EllipsisHorizontalIcon} from '@sanity/icons/EllipsisHorizontal'`
 */
export declare const EllipsisHorizontalIcon: never

/**
 * @deprecated `EllipsisVerticalIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {EllipsisVerticalIcon} from '@sanity/icons/EllipsisVertical'`
 */
export declare const EllipsisVerticalIcon: never

/**
 * @deprecated `EmptyIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {EmptyIcon} from '@sanity/icons/Empty'`
 */
export declare const EmptyIcon: never

/**
 * @deprecated `EnterIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {EnterIcon} from '@sanity/icons/Enter'`
 */
export declare const EnterIcon: never

/**
 * @deprecated `EnterRightIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {EnterRightIcon} from '@sanity/icons/EnterRight'`
 */
export declare const EnterRightIcon: never

/**
 * @deprecated `EnvelopeIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {EnvelopeIcon} from '@sanity/icons/Envelope'`
 */
export declare const EnvelopeIcon: never

/**
 * @deprecated `EqualIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {EqualIcon} from '@sanity/icons/Equal'`
 */
export declare const EqualIcon: never

/**
 * @deprecated `ErrorFilledIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {ErrorFilledIcon} from '@sanity/icons/ErrorFilled'`
 */
export declare const ErrorFilledIcon: never

/**
 * @deprecated `ErrorOutlineIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {ErrorOutlineIcon} from '@sanity/icons/ErrorOutline'`
 */
export declare const ErrorOutlineIcon: never

/**
 * @deprecated `ErrorScreenIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {ErrorScreenIcon} from '@sanity/icons/ErrorScreen'`
 */
export declare const ErrorScreenIcon: never

/**
 * @deprecated `ExpandIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {ExpandIcon} from '@sanity/icons/Expand'`
 */
export declare const ExpandIcon: never

/**
 * @deprecated `EyeClosedIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {EyeClosedIcon} from '@sanity/icons/EyeClosed'`
 */
export declare const EyeClosedIcon: never

/**
 * @deprecated `EyeOpenIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {EyeOpenIcon} from '@sanity/icons/EyeOpen'`
 */
export declare const EyeOpenIcon: never

/**
 * @deprecated `FaceHappyIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {FaceHappyIcon} from '@sanity/icons/FaceHappy'`
 */
export declare const FaceHappyIcon: never

/**
 * @deprecated `FaceIndifferentIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {FaceIndifferentIcon} from '@sanity/icons/FaceIndifferent'`
 */
export declare const FaceIndifferentIcon: never

/**
 * @deprecated `FaceSadIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {FaceSadIcon} from '@sanity/icons/FaceSad'`
 */
export declare const FaceSadIcon: never

/**
 * @deprecated `FeedbackIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {FeedbackIcon} from '@sanity/icons/Feedback'`
 */
export declare const FeedbackIcon: never

/**
 * @deprecated `FilterIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {FilterIcon} from '@sanity/icons/Filter'`
 */
export declare const FilterIcon: never

/**
 * @deprecated `FolderIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {FolderIcon} from '@sanity/icons/Folder'`
 */
export declare const FolderIcon: never

/**
 * @deprecated `GenerateIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {GenerateIcon} from '@sanity/icons/Generate'`
 */
export declare const GenerateIcon: never

/**
 * @deprecated `GithubIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {GithubIcon} from '@sanity/icons/Github'`
 */
export declare const GithubIcon: never

/**
 * @deprecated `GroqIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {GroqIcon} from '@sanity/icons/Groq'`
 */
export declare const GroqIcon: never

/**
 * @deprecated `HashIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {HashIcon} from '@sanity/icons/Hash'`
 */
export declare const HashIcon: never

/**
 * @deprecated `HeartFilledIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {HeartFilledIcon} from '@sanity/icons/HeartFilled'`
 */
export declare const HeartFilledIcon: never

/**
 * @deprecated `HeartIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {HeartIcon} from '@sanity/icons/Heart'`
 */
export declare const HeartIcon: never

/**
 * @deprecated `HelpCircleIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {HelpCircleIcon} from '@sanity/icons/HelpCircle'`
 */
export declare const HelpCircleIcon: never

/**
 * @deprecated `HighlightIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {HighlightIcon} from '@sanity/icons/Highlight'`
 */
export declare const HighlightIcon: never

/**
 * @deprecated `HomeIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {HomeIcon} from '@sanity/icons/Home'`
 */
export declare const HomeIcon: never

/**
 * @deprecated `IceCreamIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {IceCreamIcon} from '@sanity/icons/IceCream'`
 */
export declare const IceCreamIcon: never

/**
 * @deprecated `ImageIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {ImageIcon} from '@sanity/icons/Image'`
 */
export declare const ImageIcon: never

/**
 * @deprecated `ImageRemoveIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {ImageRemoveIcon} from '@sanity/icons/ImageRemove'`
 */
export declare const ImageRemoveIcon: never

/**
 * @deprecated `ImagesIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {ImagesIcon} from '@sanity/icons/Images'`
 */
export declare const ImagesIcon: never

/**
 * @deprecated `InboxIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {InboxIcon} from '@sanity/icons/Inbox'`
 */
export declare const InboxIcon: never

/**
 * @deprecated `InfoFilledIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {InfoFilledIcon} from '@sanity/icons/InfoFilled'`
 */
export declare const InfoFilledIcon: never

/**
 * @deprecated `InfoOutlineIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {InfoOutlineIcon} from '@sanity/icons/InfoOutline'`
 */
export declare const InfoOutlineIcon: never

/**
 * @deprecated `InlineElementIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {InlineElementIcon} from '@sanity/icons/InlineElement'`
 */
export declare const InlineElementIcon: never

/**
 * @deprecated `InlineIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {InlineIcon} from '@sanity/icons/Inline'`
 */
export declare const InlineIcon: never

/**
 * @deprecated `InsertAboveIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {InsertAboveIcon} from '@sanity/icons/InsertAbove'`
 */
export declare const InsertAboveIcon: never

/**
 * @deprecated `InsertBelowIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {InsertBelowIcon} from '@sanity/icons/InsertBelow'`
 */
export declare const InsertBelowIcon: never

/**
 * @deprecated `ItalicIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {ItalicIcon} from '@sanity/icons/Italic'`
 */
export declare const ItalicIcon: never

/**
 * @deprecated `JoystickIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {JoystickIcon} from '@sanity/icons/Joystick'`
 */
export declare const JoystickIcon: never

/**
 * @deprecated `JsonIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {JsonIcon} from '@sanity/icons/Json'`
 */
export declare const JsonIcon: never

/**
 * @deprecated `LaunchIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {LaunchIcon} from '@sanity/icons/Launch'`
 */
export declare const LaunchIcon: never

/**
 * @deprecated `LeaveIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {LeaveIcon} from '@sanity/icons/Leave'`
 */
export declare const LeaveIcon: never

/**
 * @deprecated `LemonIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {LemonIcon} from '@sanity/icons/Lemon'`
 */
export declare const LemonIcon: never

/**
 * @deprecated `LinkIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {LinkIcon} from '@sanity/icons/Link'`
 */
export declare const LinkIcon: never

/**
 * @deprecated `LinkRemovedIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {LinkRemovedIcon} from '@sanity/icons/LinkRemoved'`
 */
export declare const LinkRemovedIcon: never

/**
 * @deprecated `LinkedinIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {LinkedinIcon} from '@sanity/icons/Linkedin'`
 */
export declare const LinkedinIcon: never

/**
 * @deprecated `ListIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {ListIcon} from '@sanity/icons/List'`
 */
export declare const ListIcon: never

/**
 * @deprecated `LockIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {LockIcon} from '@sanity/icons/Lock'`
 */
export declare const LockIcon: never

/**
 * @deprecated `LogoJsIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {LogoJsIcon} from '@sanity/icons/LogoJs'`
 */
export declare const LogoJsIcon: never

/**
 * @deprecated `LogoTsIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {LogoTsIcon} from '@sanity/icons/LogoTs'`
 */
export declare const LogoTsIcon: never

/**
 * @deprecated `MarkerIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {MarkerIcon} from '@sanity/icons/Marker'`
 */
export declare const MarkerIcon: never

/**
 * @deprecated `MarkerRemovedIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {MarkerRemovedIcon} from '@sanity/icons/MarkerRemoved'`
 */
export declare const MarkerRemovedIcon: never

/**
 * @deprecated `MasterDetailIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {MasterDetailIcon} from '@sanity/icons/MasterDetail'`
 */
export declare const MasterDetailIcon: never

/**
 * @deprecated `MenuIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {MenuIcon} from '@sanity/icons/Menu'`
 */
export declare const MenuIcon: never

/**
 * @deprecated `MicrophoneIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {MicrophoneIcon} from '@sanity/icons/Microphone'`
 */
export declare const MicrophoneIcon: never

/**
 * @deprecated `MicrophoneSlashIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {MicrophoneSlashIcon} from '@sanity/icons/MicrophoneSlash'`
 */
export declare const MicrophoneSlashIcon: never

/**
 * @deprecated `MobileDeviceIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {MobileDeviceIcon} from '@sanity/icons/MobileDevice'`
 */
export declare const MobileDeviceIcon: never

/**
 * @deprecated `MoonIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {MoonIcon} from '@sanity/icons/Moon'`
 */
export declare const MoonIcon: never

/**
 * @deprecated `NumberIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {NumberIcon} from '@sanity/icons/Number'`
 */
export declare const NumberIcon: never

/**
 * @deprecated `OkHandIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {OkHandIcon} from '@sanity/icons/OkHand'`
 */
export declare const OkHandIcon: never

/**
 * @deprecated `OlistIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {OlistIcon} from '@sanity/icons/Olist'`
 */
export declare const OlistIcon: never

/**
 * @deprecated `OverageIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {OverageIcon} from '@sanity/icons/Overage'`
 */
export declare const OverageIcon: never

/**
 * @deprecated `PackageIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {PackageIcon} from '@sanity/icons/Package'`
 */
export declare const PackageIcon: never

/**
 * @deprecated `PanelLeftIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {PanelLeftIcon} from '@sanity/icons/PanelLeft'`
 */
export declare const PanelLeftIcon: never

/**
 * @deprecated `PanelRightIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {PanelRightIcon} from '@sanity/icons/PanelRight'`
 */
export declare const PanelRightIcon: never

/**
 * @deprecated `PauseIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {PauseIcon} from '@sanity/icons/Pause'`
 */
export declare const PauseIcon: never

/**
 * @deprecated `PinFilledIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {PinFilledIcon} from '@sanity/icons/PinFilled'`
 */
export declare const PinFilledIcon: never

/**
 * @deprecated `PinIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {PinIcon} from '@sanity/icons/Pin'`
 */
export declare const PinIcon: never

/**
 * @deprecated `PinRemovedIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {PinRemovedIcon} from '@sanity/icons/PinRemoved'`
 */
export declare const PinRemovedIcon: never

/**
 * @deprecated `PlayIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {PlayIcon} from '@sanity/icons/Play'`
 */
export declare const PlayIcon: never

/**
 * @deprecated `PlugIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {PlugIcon} from '@sanity/icons/Plug'`
 */
export declare const PlugIcon: never

/**
 * @deprecated `PresentationIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {PresentationIcon} from '@sanity/icons/Presentation'`
 */
export declare const PresentationIcon: never

/**
 * @deprecated `Progress50Icon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {Progress50Icon} from '@sanity/icons/Progress50'`
 */
export declare const Progress50Icon: never

/**
 * @deprecated `Progress75Icon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {Progress75Icon} from '@sanity/icons/Progress75'`
 */
export declare const Progress75Icon: never

/**
 * @deprecated `ProjectsIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {ProjectsIcon} from '@sanity/icons/Projects'`
 */
export declare const ProjectsIcon: never

/**
 * @deprecated `PublishIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {PublishIcon} from '@sanity/icons/Publish'`
 */
export declare const PublishIcon: never

/**
 * @deprecated `ReadOnlyIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {ReadOnlyIcon} from '@sanity/icons/ReadOnly'`
 */
export declare const ReadOnlyIcon: never

/**
 * @deprecated `RedoIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {RedoIcon} from '@sanity/icons/Redo'`
 */
export declare const RedoIcon: never

/**
 * @deprecated `RefreshIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {RefreshIcon} from '@sanity/icons/Refresh'`
 */
export declare const RefreshIcon: never

/**
 * @deprecated `RemoveCircleIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {RemoveCircleIcon} from '@sanity/icons/RemoveCircle'`
 */
export declare const RemoveCircleIcon: never

/**
 * @deprecated `RemoveIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {RemoveIcon} from '@sanity/icons/Remove'`
 */
export declare const RemoveIcon: never

/**
 * @deprecated `ResetIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {ResetIcon} from '@sanity/icons/Reset'`
 */
export declare const ResetIcon: never

/**
 * @deprecated `RestoreIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {RestoreIcon} from '@sanity/icons/Restore'`
 */
export declare const RestoreIcon: never

/**
 * @deprecated `RetrieveIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {RetrieveIcon} from '@sanity/icons/Retrieve'`
 */
export declare const RetrieveIcon: never

/**
 * @deprecated `RetryIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {RetryIcon} from '@sanity/icons/Retry'`
 */
export declare const RetryIcon: never

/**
 * @deprecated `RevertIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {RevertIcon} from '@sanity/icons/Revert'`
 */
export declare const RevertIcon: never

/**
 * @deprecated `RobotIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {RobotIcon} from '@sanity/icons/Robot'`
 */
export declare const RobotIcon: never

/**
 * @deprecated `RocketIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {RocketIcon} from '@sanity/icons/Rocket'`
 */
export declare const RocketIcon: never

/**
 * @deprecated `SchemaIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {SchemaIcon} from '@sanity/icons/Schema'`
 */
export declare const SchemaIcon: never

/**
 * @deprecated `SearchIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {SearchIcon} from '@sanity/icons/Search'`
 */
export declare const SearchIcon: never

/**
 * @deprecated `SelectIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {SelectIcon} from '@sanity/icons/Select'`
 */
export declare const SelectIcon: never

/**
 * @deprecated `ShareIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {ShareIcon} from '@sanity/icons/Share'`
 */
export declare const ShareIcon: never

/**
 * @deprecated `SortIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {SortIcon} from '@sanity/icons/Sort'`
 */
export declare const SortIcon: never

/**
 * @deprecated `SparkleIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {SparkleIcon} from '@sanity/icons/Sparkle'`
 */
export declare const SparkleIcon: never

/**
 * @deprecated `SparklesIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {SparklesIcon} from '@sanity/icons/Sparkles'`
 */
export declare const SparklesIcon: never

/**
 * @deprecated `SpinnerIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {SpinnerIcon} from '@sanity/icons/Spinner'`
 */
export declare const SpinnerIcon: never

/**
 * @deprecated `SplitHorizontalIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {SplitHorizontalIcon} from '@sanity/icons/SplitHorizontal'`
 */
export declare const SplitHorizontalIcon: never

/**
 * @deprecated `SplitVerticalIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {SplitVerticalIcon} from '@sanity/icons/SplitVertical'`
 */
export declare const SplitVerticalIcon: never

/**
 * @deprecated `SquareIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {SquareIcon} from '@sanity/icons/Square'`
 */
export declare const SquareIcon: never

/**
 * @deprecated `StackCompactIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {StackCompactIcon} from '@sanity/icons/StackCompact'`
 */
export declare const StackCompactIcon: never

/**
 * @deprecated `StackIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {StackIcon} from '@sanity/icons/Stack'`
 */
export declare const StackIcon: never

/**
 * @deprecated `StarFilledIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {StarFilledIcon} from '@sanity/icons/StarFilled'`
 */
export declare const StarFilledIcon: never

/**
 * @deprecated `StarIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {StarIcon} from '@sanity/icons/Star'`
 */
export declare const StarIcon: never

/**
 * @deprecated `StopIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {StopIcon} from '@sanity/icons/Stop'`
 */
export declare const StopIcon: never

/**
 * @deprecated `StrikethroughIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {StrikethroughIcon} from '@sanity/icons/Strikethrough'`
 */
export declare const StrikethroughIcon: never

/**
 * @deprecated `StringIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {StringIcon} from '@sanity/icons/String'`
 */
export declare const StringIcon: never

/**
 * @deprecated `SunIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {SunIcon} from '@sanity/icons/Sun'`
 */
export declare const SunIcon: never

/**
 * @deprecated `SyncIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {SyncIcon} from '@sanity/icons/Sync'`
 */
export declare const SyncIcon: never

/**
 * @deprecated `TabletDeviceIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {TabletDeviceIcon} from '@sanity/icons/TabletDevice'`
 */
export declare const TabletDeviceIcon: never

/**
 * @deprecated `TagIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {TagIcon} from '@sanity/icons/Tag'`
 */
export declare const TagIcon: never

/**
 * @deprecated `TagsIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {TagsIcon} from '@sanity/icons/Tags'`
 */
export declare const TagsIcon: never

/**
 * @deprecated `TargetIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {TargetIcon} from '@sanity/icons/Target'`
 */
export declare const TargetIcon: never

/**
 * @deprecated `TaskIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {TaskIcon} from '@sanity/icons/Task'`
 */
export declare const TaskIcon: never

/**
 * @deprecated `TerminalIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {TerminalIcon} from '@sanity/icons/Terminal'`
 */
export declare const TerminalIcon: never

/**
 * @deprecated `TextIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {TextIcon} from '@sanity/icons/Text'`
 */
export declare const TextIcon: never

/**
 * @deprecated `ThLargeIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {ThLargeIcon} from '@sanity/icons/ThLarge'`
 */
export declare const ThLargeIcon: never

/**
 * @deprecated `ThListIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {ThListIcon} from '@sanity/icons/ThList'`
 */
export declare const ThListIcon: never

/**
 * @deprecated `ThumbsDownIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {ThumbsDownIcon} from '@sanity/icons/ThumbsDown'`
 */
export declare const ThumbsDownIcon: never

/**
 * @deprecated `ThumbsUpIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {ThumbsUpIcon} from '@sanity/icons/ThumbsUp'`
 */
export declare const ThumbsUpIcon: never

/**
 * @deprecated `TiersIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {TiersIcon} from '@sanity/icons/Tiers'`
 */
export declare const TiersIcon: never

/**
 * @deprecated `TimelineIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {TimelineIcon} from '@sanity/icons/Timeline'`
 */
export declare const TimelineIcon: never

/**
 * @deprecated `ToggleArrowRightIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {ToggleArrowRightIcon} from '@sanity/icons/ToggleArrowRight'`
 */
export declare const ToggleArrowRightIcon: never

/**
 * @deprecated `TokenIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {TokenIcon} from '@sanity/icons/Token'`
 */
export declare const TokenIcon: never

/**
 * @deprecated `TransferIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {TransferIcon} from '@sanity/icons/Transfer'`
 */
export declare const TransferIcon: never

/**
 * @deprecated `TranslateIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {TranslateIcon} from '@sanity/icons/Translate'`
 */
export declare const TranslateIcon: never

/**
 * @deprecated `TrashIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {TrashIcon} from '@sanity/icons/Trash'`
 */
export declare const TrashIcon: never

/**
 * @deprecated `TrendUpwardIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {TrendUpwardIcon} from '@sanity/icons/TrendUpward'`
 */
export declare const TrendUpwardIcon: never

/**
 * @deprecated `TriangleOutlineIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {TriangleOutlineIcon} from '@sanity/icons/TriangleOutline'`
 */
export declare const TriangleOutlineIcon: never

/**
 * @deprecated `TrolleyIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {TrolleyIcon} from '@sanity/icons/Trolley'`
 */
export declare const TrolleyIcon: never

/**
 * @deprecated `TruncateIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {TruncateIcon} from '@sanity/icons/Truncate'`
 */
export declare const TruncateIcon: never

/**
 * @deprecated `TwitterIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {TwitterIcon} from '@sanity/icons/Twitter'`
 */
export declare const TwitterIcon: never

/**
 * @deprecated `UlistIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {UlistIcon} from '@sanity/icons/Ulist'`
 */
export declare const UlistIcon: never

/**
 * @deprecated `UnarchiveIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {UnarchiveIcon} from '@sanity/icons/Unarchive'`
 */
export declare const UnarchiveIcon: never

/**
 * @deprecated `UnderlineIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {UnderlineIcon} from '@sanity/icons/Underline'`
 */
export declare const UnderlineIcon: never

/**
 * @deprecated `UndoIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {UndoIcon} from '@sanity/icons/Undo'`
 */
export declare const UndoIcon: never

/**
 * @deprecated `UnknownIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {UnknownIcon} from '@sanity/icons/Unknown'`
 */
export declare const UnknownIcon: never

/**
 * @deprecated `UnlinkIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {UnlinkIcon} from '@sanity/icons/Unlink'`
 */
export declare const UnlinkIcon: never

/**
 * @deprecated `UnlockIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {UnlockIcon} from '@sanity/icons/Unlock'`
 */
export declare const UnlockIcon: never

/**
 * @deprecated `UnpublishIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {UnpublishIcon} from '@sanity/icons/Unpublish'`
 */
export declare const UnpublishIcon: never

/**
 * @deprecated `UploadIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {UploadIcon} from '@sanity/icons/Upload'`
 */
export declare const UploadIcon: never

/**
 * @deprecated `UserIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {UserIcon} from '@sanity/icons/User'`
 */
export declare const UserIcon: never

/**
 * @deprecated `UsersIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {UsersIcon} from '@sanity/icons/Users'`
 */
export declare const UsersIcon: never

/**
 * @deprecated `VersionsIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {VersionsIcon} from '@sanity/icons/Versions'`
 */
export declare const VersionsIcon: never

/**
 * @deprecated `VideoIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {VideoIcon} from '@sanity/icons/Video'`
 */
export declare const VideoIcon: never

/**
 * @deprecated `WarningFilledIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {WarningFilledIcon} from '@sanity/icons/WarningFilled'`
 */
export declare const WarningFilledIcon: never

/**
 * @deprecated `WarningOutlineIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {WarningOutlineIcon} from '@sanity/icons/WarningOutline'`
 */
export declare const WarningOutlineIcon: never

/**
 * @deprecated `WrenchIcon` is no longer exported from the `@sanity/icons` root entry (removed in v5) – the icon itself still exists. Import it from its own subpath instead: `import {WrenchIcon} from '@sanity/icons/Wrench'`
 */
export declare const WrenchIcon: never
