# System classes

System classes apply custom properties defined in our design tokens library. They do not apply custom properties defined locally to the component library, and they do not apply CSS keyword values.
