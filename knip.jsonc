{
  "$schema": "https://unpkg.com/knip@6/schema.json",
  "workspaces": {
    ".": {},
    "apps/blueprints/docs": {
      // The Blueprint config and function handlers are runtime entry points
      // deployed via @sanity/runtime-cli, not imported by application code
      "entry": ["sanity.blueprint.ts", "functions/*/index.ts"],
    },
    "apps/docs": {},
    "apps/icons": {},
    "packages/color": {
      // The palette generator is invoked via the package's `generate` script,
      // not imported by application code
      "entry": ["scripts/generate.ts"],
    },
    "packages/icons": {
      // The generate/seed package.json scripts run these through unrun, which
      // knip does not recognize as a script runner
      "entry": ["scripts/*.ts"],
    },
    "packages/ui": {
      "ignoreDependencies": [
        // babel-plugin-react-compiler (target '18') injects imports of this runtime
        // package into the compiled dist output, so it is never imported in source
        "react-compiler-runtime",
      ],
    },
  },
}
