import {layer} from '@vanilla-extract/css'

/** @public */
export const layers = {
  theme: layer('theme'),
  props: layer('props'),
  primitives: layer('primitives'),
  components: layer('components'),
}
