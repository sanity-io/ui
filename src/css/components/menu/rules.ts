import {Rules} from '../../types'

export const menuRules: Rules = {
  'menu': {
    outline: 'none',
  },

  'menu-divider': {
    height: `1px`,
    border: 0,
    backgroundColor: 'var(--card-border-color)',
    margin: 0,
  },
}
