export const DEFAULT_META_DESCRIPTION = 'An ergonomic toolkit to design with code.'
// Resolved against `metadataBase` (the production origin), so the /ui
// basePath has to be part of the path
export const DEFAULT_META_OG_IMAGE = '/ui/images/social-default.png'
