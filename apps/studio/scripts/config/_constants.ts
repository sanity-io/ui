import path from 'path'

export const ROOT_PATH = path.resolve(__dirname, '../..')
export const CONFIG_TS_PATH = path.resolve(ROOT_PATH, 'sanity.config.ts')
export const CONFIG_JSON_PATH = path.resolve(ROOT_PATH, 'sanity.json')
