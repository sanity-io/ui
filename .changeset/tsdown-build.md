---
"@sanity/ui": patch
---

The package is now built with [tsdown](https://tsdown.dev). It continues to ship both CJS (`require`) and ESM (`import`) builds with identical entry points and named exports.
