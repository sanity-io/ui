---
"@sanity/themer": minor
---

**Node.js 22.12+ required.**

The `engines` range is raised to `>=22.12`, matching `sanity` and `@sanity/ui`. The package was already ESM-only, so nothing else changes.
